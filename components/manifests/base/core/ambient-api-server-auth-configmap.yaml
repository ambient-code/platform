---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ambient-api-server-auth
  labels:
    app: ambient-api-server
    component: auth
data:
  jwks.json: |
    {"keys":[]}
  acl.yml: |
    - claim: email
      pattern: ^.*@(redhat\.com|ambient\.code)$
