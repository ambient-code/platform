apiVersion: apps/v1
kind: Deployment
metadata:
  name: ambient-control-plane
spec:
  template:
    spec:
      containers:
      - name: ambient-control-plane
        image: image-registry.openshift-image-registry.svc:5000/vteam-dev/vteam_control_plane:latest
