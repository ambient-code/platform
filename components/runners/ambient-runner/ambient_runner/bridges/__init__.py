"""Framework-specific bridge implementations.

Available bridges:
- ``ClaudeBridge`` — Claude Agent SDK
- ``LangGraphBridge`` — LangGraph (requires ag_ui_langgraph)
- ``ReplayBridge`` — Pre-recorded AG-UI event replay (for testing)
"""
